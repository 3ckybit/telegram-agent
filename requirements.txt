# Core (Mac + Pi)
python-telegram-bot==22.8
anthropic==0.28.0
redis==5.0.3
APScheduler==3.10.4
python-dotenv==1.0.1

# Pi-only (install manually on Pi — requires ffmpeg + native build)
# faster-whisper==1.0.3

# Dev / Testing (Mac)
pytest==8.2.0
pytest-asyncio==0.23.6
